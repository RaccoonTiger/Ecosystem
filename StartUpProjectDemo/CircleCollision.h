#pragma once


//#include "Vect2d.h"

/*

.h crée par Stanley Vu
Mis à jour le Dec.11.2020

Fonctions:
	-bool Detect: Calcule la distance entre les deux positions (Cellule 1 et 2)
					     et compare la distance entre les deux position et les deux rayons combinés ensemble.
						 si le rayon combiné est plus grand ou égal à la distance entre les deux points,
						 il y a une collision (retourne vrai).

*/

/*

class CircleCollision
{
public:

	bool Detect(Vect2d a, Vect2d b, float radiusA, float radiusB);

};

*/